# what is conlog

Alternative to `console.log`

# installation

`npm i conlog --save`

# Usage

Instead of:

```
console.log(myVariable);
```

Use:

```
conlog(myVariable);
or
conlog(myVariable, 'some extra description -- optional');
```
